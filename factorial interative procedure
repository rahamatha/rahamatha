#include <stdio.h>
unsigned long long factorial(int n)
{
    unsigned long long result = 1;
    for (int i = 1; i <= n; ++i)
    {
        result *= i;
    }
    return result;
}
int main()
{
    int n = 3;
    printf("Input: %d\n", n);
    printf("Output: %llu\n", factorial(n));
    return 0;
}
